class Solution(object):
    def merge(self,nums1,m,nums2,n):
        i=0
        while i<n:
            nums1.insert(i,nums2[i])
            i+=1
            nums1.pop()
        nums1.sort()
        
